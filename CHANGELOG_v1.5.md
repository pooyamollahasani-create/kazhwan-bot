# Kazhwan Bot v1.5

- تعریف سفر مستقل از گروه تلگرام از پنل خصوصی مدیر.
- افزودن دستی مسافر به هر سفر با نام و نام خانوادگی و شماره اختیاری.
- اگر پروفایل KZH موجود باشد، سابقه مستقیم به همان پروفایل متصل می‌شود.
- اگر پروفایل وجود نداشته باشد، مسافر به‌صورت Guest Traveler ذخیره می‌شود.
- امتیاز سفر مسافر موقت تا زمان اتصال به پروفایل واقعی به‌صورت معوق نگهداری می‌شود.
- پس از ثبت‌نام واقعی، تطبیق بر اساس نام و/یا موبایل به مدیر پیشنهاد می‌شود؛ اتصال فقط با تأیید مدیر انجام می‌شود.
- بعد از اتصال سابقه موقت، سفرها و امتیازهای معوق به پروفایل KZH منتقل می‌شوند.
- بعد از تأیید «شرکت کرده» برای مسافر دارای پروفایل، پیام خصوصی تأیید سفر و امتیاز برای او ارسال می‌شود.
- خروجی Excel سفرها، مسافران موقت را نیز نمایش می‌دهد.
- خطاهای داخلی دیگر به‌صورت پیام عمومی در گروه نمایش داده نمی‌شوند و فقط در Railway Log ثبت می‌شوند.
