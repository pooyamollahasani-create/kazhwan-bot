# Kazhwan Bot v1.4 — مدیریت و سفرها

## پنل مدیریتی

- `/admin` پنل دکمه‌ای مدیریت
- `/stats` آمار اعضای ثبت‌شده BTC، غیرفعال‌ها، معرفی‌ها و امتیازها
- `/members` و `/registered` تعداد اعضای ثبت‌شده BTC
- `/unregistered` اعضایی که ربات در گروه دیده ولی هنوز پروفایل ندارند
- `/member <query>` جستجو با نام، شماره، کد عضویت، کد معرف یا Telegram ID
- `/inactive30` و `/inactive60`
- `/topreferrals` و `/referrals`
- `/exportmembers` خروجی Excel اعضای BTC
- `/exportinactive` خروجی Excel غیرفعال‌ها
- `/exportreferrals` خروجی Excel معرف‌ها
- `/exportall` خروجی کامل اعضای BTC

## مدیریت سفرها از داخل گروه سفر

- `/settrip` تعریف یا به‌روزرسانی سفر برای همان گروه
- `/tripinfo` اطلاعات سفر و تعداد اعلام حضورها
- `/tripregister` انتشار دکمه «ثبت سفر در پروفایل من»
- `/tripparticipants` فهرست مسافران ثبت‌شده
- `/exporttrip` خروجی Excel مسافران همان سفر
- `/tripclose` بستن ثبت حضور
- `/tripopen` بازکردن ثبت حضور
- `/endtrip` پایان سفر
- `/tripattend` ثبت حضور واقعی یک مسافر (با Reply یا کد/شماره)
- `/tripcancel` ثبت انصراف یک مسافر (با Reply یا کد/شماره)

## پروفایل مسافر

- ورود از دکمه سفر با Deep Link به PV ربات
- اگر پروفایل کامل باشد، فقط تأیید ثبت سفر گرفته می‌شود
- اگر پروفایل ناقص باشد، ابتدا فرم فعلی تکمیل و سپس ثبت سفر پیشنهاد می‌شود
- سفرها در «تاریخچه فعالیت‌های من» با وضعیت اعلام حضور/شرکت کرده/انصراف نمایش داده می‌شوند

## دیتابیس

دو جدول جدید بدون حذف یا بازسازی اطلاعات فعلی ساخته می‌شود:

- `trips`
- `trip_participants`

جدول‌های قبلی و اطلاعات PostgreSQL دست‌نخورده باقی می‌مانند.
