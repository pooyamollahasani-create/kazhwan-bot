# Kazhwan Bot v1.2

## Added
- دریافت `Chat Join Request` گروه اصلی
- ارسال پیام خصوصی به درخواست‌دهنده پیش از تأیید عضویت
- فرم مرحله‌ای: نام، موبایل، شهر، نحوه آشنایی
- سؤال اختیاری کد معرف (بله/خیر)
- اعتبارسنجی کد معرف و بررسی حضور معرف در گروه
- کد معرف اختصاصی برای هر عضو
- ۱۰ امتیاز برای معرف پس از عضویت موفق شخص معرفی‌شده
- ذخیره درخواست عضویت pending در PostgreSQL برای مقاومت در برابر ری‌استارت Railway
- تأیید خودکار Join Request فقط پس از تکمیل فرم، عضویت کانال و پذیرش قوانین
- جلوگیری از اجرای فرم خصوصی در خود گروه
- نمایش کد معرف و تعداد معرفی‌های موفق در پروفایل

## Safe database migration
اطلاعات جدول فعلی `users` حذف نمی‌شود. ستون‌های جدید در startup به‌صورت idempotent اضافه می‌شوند و برای کاربران قدیمی کد معرف ساخته می‌شود.

## Required Railway variable
`GROUP_CHAT_ID` باید روی شناسه عددی گروه اصلی تنظیم باشد.
