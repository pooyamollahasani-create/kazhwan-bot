# v1.4.3 — Private Trip Management

- تمام مدیریت سفرها به PV مدیر منتقل شد.
- `/admin` دارای بخش «مدیریت سفرها» است.
- انتخاب سفر، مشاهده مسافران، تأیید شرکت، انصراف، بازگشت به اعلام حضور، باز/بسته کردن ثبت، پایان سفر و خروجی Excel از PV انجام می‌شود.
- امتیاز سفر فقط با وضعیت `attended` اعمال می‌شود و تکراری نیست.
- اگر وضعیت `attended` اصلاح شود، امتیاز سفر قبلی به‌صورت امن برگشت داده می‌شود.
- دستورات مدیریتی سفر در گروه فقط مدیر را به پنل خصوصی هدایت می‌کنند.
- `/settrip`، `/tripinfo` و `/tripregister` همچنان داخل گروه سفر کار می‌کنند.
